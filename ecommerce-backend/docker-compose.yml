version: "3.7"

services:
  sql-db:
    container_name: sql-ecommerce
    image: mysql
    expose:
      - 3306
    ports:
      - 3317:3306
    restart: always
    environment:
      MYSQL_DATABASE: "ecommerce"
      MYSQL_ROOT_PASSWORD: "admin"